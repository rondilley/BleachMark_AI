"""Effectiveness harness (FR-44).

The tool has no watermark key, so the harness uses reference generators or test
keys to make watermarked samples. It attacks them and measures the detection rate
and the bleach rate for each stego method. This turns the success criteria into
measured numbers, not a self-graded exam.
"""
