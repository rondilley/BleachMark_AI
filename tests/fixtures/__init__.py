"""Test fixtures for BleachMark (TR-01, TR-02).

Fixtures are built with chr() so no source file holds a literal invisible or
confusable character. Each carrier has positive fixtures (the signal must fire) and
legitimate-use fixtures (the signal must stay silent), per TR-02.
"""
